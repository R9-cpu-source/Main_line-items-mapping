# Runtime
# Preferred PDF text engine is Poppler's `pdftotext -layout` (system package:
#   Debian/Ubuntu: apt-get install poppler-utils
#   macOS:        brew install poppler
# ). If Poppler is not present the extractor falls back to PyMuPDF:
PyMuPDF>=1.24        # fallback PDF text engine (fitz)
# XLSX reading uses only the standard library (zipfile + ElementTree).
